apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: eureka
  namespace: osiguranje-prod
  labels:
    service: eureka
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
