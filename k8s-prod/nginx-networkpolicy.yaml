apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: nginx
  namespace: osiguranje-prod
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              network/nginx: "true"
  podSelector:
    matchLabels:
      network/nginx: "true"
