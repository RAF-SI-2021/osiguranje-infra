apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gateway
  namespace: osiguranje-dev
  labels:
    service: gateway
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
